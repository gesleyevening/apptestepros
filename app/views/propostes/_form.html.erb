<%= form_with(model: proposte, local: true) do |form| %>
  <% if proposte.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(proposte.errors.count, "error") %> prohibited this proposte from being saved:</h2>

      <ul>
      <% proposte.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome %>
  </div>

  <div class="field">
    <%= form.label :descricao %>
    <%= form.text_area :descricao %>
  </div>

  <div class="field">
    <%= form.label :valor %>
    <%= form.text_field :valor %>
  </div>

  <div class="field">
    <%= form.label :dataInicio %>
    <%= form.datetime_select :dataInicio %>
  </div>

  <div class="field">
    <%= form.label :dataFim %>
    <%= form.datetime_select :dataFim %>
  </div>

  <div class="field">
    <%= form.label :cidade %>
    <%= form.text_field :cidade %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
